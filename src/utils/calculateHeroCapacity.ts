// 📄 src/utils/calculateHeroCapacity.ts

type HeroRarity = 'normal' | 'good' | 'rare' | 'epic' | 'legendary';

export function calculateHeroCapacity(level: number, rarity: HeroRarity): number {
  const baseHero
}